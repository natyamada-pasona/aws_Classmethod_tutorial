# 02：API 駆動の開発と同期イベントソース

Lambda の invoke コマンドは、AWS CLI から Lambda 関数を直接呼び出すためのコマンドです。

```cmd
# 同期呼び出し（デフォルト）- レスポンスを待つ
aws lambda invoke --function-name my-function \
  --invocation-type RequestResponse \
  response_output.json

# 非同期呼び出し - レスポンスを待たない
aws lambda invoke --function-name my-function \
  --invocation-type Event \
  response_output.json

```


## アクセスパターンに応じたエンドポイントの選択

API Gatewayには3つのエンドポイントタイプがあり、「誰がどこからAPIを叩くか」で選ぶ。

- エッジ最適化: 世界中からアクセスがある場合。CloudFrontのエッジロケーション（世界各地のキャッシュ拠点）を経由するのでレイテンシーが下がる。デフォルト。
- リージョナル: 同じリージョン内からのアクセスが中心の場合。
- プライベート: VPC内部からのみアクセスさせたい場合。

レイテンシーとコストを最適化できる



## HTTP API と REST API の違い
RESTful APIとは異なる概念。「HTTP API」「REST API」はAWSのサービス名を指す。

| | HTTP API | REST API |
|---|---|---|
| コスト | 安い（100万リクエストあたり約$1.00） | 高い（100万リクエストあたり約$3.50） |
| レイテンシー | 低い | HTTP APIより高い |
| 認証 | JWT、OIDC | JWT、OIDC、IAM、Cognito、APIキー |
| エンドポイント | リージョナルのみ | エッジ最適化・リージョナル・プライベート |
| 選び方 | 基本はこっちでOK | HTTP APIで足りない機能があるとき |


![](/img/Gateway特徴.png)

### 認証と認可

![](/img/Gawawayと認証認可.png)

### フェデレーティッドアイデンティティ

「Cognitoユーザープールで認証 → フェデレーティッドアイデンティティで一時的なIAM認証情報に変換 → IAMポリシーで認可」という2段階の仕組み。

#### 流れ

1. クライアントが Cognito ユーザープールに認証
2. ユーザープールから JWT（IDトークン）を取得
3. JWT を使って Cognito フェデレーティッドアイデンティティに一時的な AWS 認証情報をリクエスト
4. フェデレーティッドアイデンティティが ID トークンを検証
5. 一時的な IAM 認証情報（アクセスキー・シークレットキー・セッショントークン）を発行
6. クライアントがその認証情報でペイロードに署名（SigV4署名）
7. 署名付きリクエストで API Gateway を呼び出す
8. API Gateway が IAM に問い合わせ、一時的な認証情報に紐づく IAM ポリシーを確認
9. 認可が通れば Lambda 関数を呼び出し
10. Lambda が DynamoDB 等の AWS リソースにアクセス

#### メリット

- クライアントに長期的な AWS 認証情報を持たせなくてよい（一時的なので期限切れになる）
- IAM ポリシーできめ細かいアクセス制御が可能（APIパス・メソッド単位など）
- 認証（ユーザープール）と認可（IAM）を分離できる

ポイント：JWT（この人は誰か）を IAM の一時的な認証情報（この人は何ができるか）に変換するのがフェデレーティッドアイデンティティの役割。


AWS　SAM CLI 便利！

```
# プロジェクトの初期化（テンプレートから雛形を作成）
sam init

# ローカルで Lambda 関数をテスト
sam local invoke MyFunction

# ローカルで API Gateway をエミュレート
sam local start-api

# ビルド（依存関係の解決・パッケージング）
sam build

# デプロイ（ガイド付き）
sam deploy --guided
```


1. 分散サービス ≒ マイクロサービスという理解で大丈夫でしょうか。
分散サービスは複数のサーバーで処理を分ける仕組みとなります。必ずマイクロサービスとは限りません。
システムを動かすうえで、Webサーバー、アプリサーバー、DBサーバーと分けることも分散サービスと呼びます。

2. イベント駆動開発の際、プロデューサー側の結果整合性をどのように確認するものでしょうか。プロデューサーがずっと処理を終えらなかったり、密かに失敗しているなど、
リアルタイムで処理が完了したかを確認することはせず、失敗したら再試行、デッドレターキューで確認、ログを見るなどで対応します。ログを見る、ということが観測という言葉につながるといえると思います。

1. APIをLambdaで実装すると１サービスで数十、数百以上のAPIが必要になるかと思います。
運用的な観点でこれらの管理はどう行うのが一般的でしょうか？AWS SAMをつかうイメージでしょうか。

そうですね、おっしゃる通り数が増えるとコンソールでは難しいのでSAMで管理でもよいと思います。

2. AWS SAMはサーバレス特化という理解ですが、AWS CDKでも同じことができるかと思います。主はサーバレスでも、１つでもAuroraなど非サーバレスのサービスがあると、結局AWS CDKを使うことになりそうです。１つの案件で２つのIaCを使うのは管理が複雑になるイメージがありますが、そこまで気にならないものでしょうか。

案件にもよると思いますが、確かに２つ使うより統一したいですよね。ただ、SAMでAuroraも作れはするのでテンプレが長くなることが問題なければSAMで統一もできなくはない、という感じです。
ただ統一することが望ましいとは思うので、その場合はCDKで統一がベストでしょうか。（最近はCDKが増えているようです）