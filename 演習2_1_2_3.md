# 演習2：EventBridge によるメッセージのファンアウト

## 概要

本演習では、社内で共有される技術記事や参考リンク（ブックマーク）を題材として、イベント駆動アーキテクチャの基本的な設計と実装を学習します。

この演習で扱う「記事」とは、ブログ記事やドキュメントを新しく作成するものではなく、業務中に見つけた有用な Web ページ（URL）をチーム内で共有するナレッジ情報を指します。

### 想定フロー

1. エンジニアが業務に役立つ技術記事や公式ドキュメントを見つける
2. その URL を社内アプリに登録する
3. 個人用メモではなく「チームに共有する」操作を行う

この **「記事がチームに共有された」** という出来事をシステム上の **イベント** として扱います。

### 記事共有時に発生する処理

記事が共有された際、業務上は複数の処理が発生します。

- 管理者や関係者へ共有通知を送りたい
- 共有履歴としてログを残したい
- ナレッジ共有促進のため、キャンペーン（抽選）処理を実行したい

本演習では、これらの処理を1つのプログラムにまとめて実装するのではなく、イベントを起点として疎結合に分離する設計を採用します。

### 使用するAWSサービス

| サービス | 役割 |
|---|---|
| Amazon DynamoDB | 記事（ブックマーク）情報を保存するデータストア |
| Amazon DynamoDB Streams | 記事の追加・更新をイベントとして検知 |
| AWS Lambda | DynamoDB の変更イベントを受け取り、イベントを整形して送信 |
| Amazon EventBridge | 「記事が共有された」という事実を複数の処理に分配 |
| Amazon SNS | 記事共有時に E メール通知を送信 |
| Amazon CloudWatch Logs | イベント内容をログとして記録 |

---

## タスク1：DynamoDB テーブル作成

① DynamoDB コンソールで、「テーブル作成」を選択します。

② テーブル名 `ArticlesTable`、パーティションキー `id`（文字列）を設定し、テーブルを作成します。

③ 作成したテーブルの「エクスポートとストリーム」タブで「DynamoDB Stream」を「オンにする」に設定し有効化します。
- 「ビュータイプ」は **「新旧イメージ」** を選択します。

---

## タスク2：Lambda 関数作成①（EventBridge 用）

DynamoDB テーブルに新しい記事が追加されたとき、その記事が共有される場合だけ（`shared=true`）、EventBridge にイベントを送信する関数を作成します。

① Lambda コンソール画面を表示し、関数の作成を実行します。

② 関数名は `StreamToEventBridge` とし、ランタイムは **Python 3.12** を選択します。

③ 実行ロールの設定：
- 基本的な Lambda 関数の権限 `AWSLambdaBasicExecutionRole` を付与し、ログを CloudWatch に出力できるようにします。
- さらに、EventBridge にイベントを送る権限も必要です。
  - イベントソースに「EventBridge」を選択
  - 必要なアクションには `events:PutEvents` を選択
  - 今回は演習のため、対象リソースは「全て（＊）」を選択
- Lambda が DynamoDB Stream を読むための権限 `AWSLambdaDynamoDBExecutionRole` も追加します。

④ 関数のコードは以下に置き換え、デプロイも忘れずに実行します。

---

## タスク3：Lambda と DynamoDB Stream の接続

DynamoDB のテーブルに新規作成があれば（記事が投稿されたら）Lambda 関数が実行されるようにトリガーの設定をします。

① 作成済み Lambda 関数のコンソール画面で「設定」タブを選択します。

② 「トリガー」タブを選択し、「トリガーを追加」ボタンをクリックします。

③ ソースで「DynamoDB」を選択し、`BookmarksTable` を選びます。

④ バッチサイズを **5** に設定し追加します。
