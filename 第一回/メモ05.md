# 05：Amazon EventBridge と Amazon SNS を使用してコンポーネントを疎結合化する

## 非同期通信モデル

- 同期モデル：クライアントがリクエストを送り、結果が返るまで待つ
- 非同期モデル：リクエストを投げたら待たずに先に進む

### 流れ
1. クライアント → サービスA にリクエスト
2. サービスA → サービスB にイベント通知（結果を待たない）
3. クライアントは「受付完了」だけ受け取り、次の処理へ

### メリット
- 下流の遅延・エラーに影響されず応答が速い
- タイムアウトやエラーハンドリングのロジックが減る
- 柔軟で耐障害性の高い設計になる

### デメリット
- サービスB → サービスA への返却手段がない（一方通行）

### ダウンストリームの依存とは
- ダウンストリーム（下流）＝あるサービスの先にあるサービス（例：サービスA → サービスB のB側）
- 同期モデルでは、下流サービスが遅い・落ちると上流も巻き添えで止まる → これが「ダウンストリームへの依存」
- 非同期モデルでは、イベントを投げるだけで結果を待たないのでこの依存が減る

## EventBridge スキーマレジストリ
仕様書を置く場所、のイメージ

- イベントの構造（フィールド名・型）を自動検出・保存する機能
- イベントが流れると、そのスキーマを自動生成してくれる

### 用途
- イベント構造の把握：チーム間で「このイベントにどんなデータがあるか」を確認
- コードバインディング生成：スキーマからJava/Python/TypeScript用の型定義を自動生成 → 型安全な処理が書ける
- 変更管理：スキーマのバージョン管理でイベント構造の変更を追跡

### 例：こんなイベントが流れると…

```json
{
  "source": "order-service",
  "detail-type": "OrderPlaced",
  "detail": {
    "orderId": "123",
    "amount": 5000,
    "customerId": "abc"
  }
}
```

スキーマレジストリが自動的にこういうスキーマを生成・保存する：

```json
{
  "type": "object",
  "properties": {
    "orderId":    { "type": "string" },
    "amount":     { "type": "number" },
    "customerId": { "type": "string" }
  }
}
```

→ これを元にTypeScript等の型定義を自動生成できる：

```typescript
interface OrderPlaced {
  orderId: string;
  amount: number;
  customerId: string;
}
```
