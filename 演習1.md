# 【演習１】サーバーレスのウェブアプリを構築する

## タスク１：継続的デプロイを使用した静的ウェブホスティング

AWS Amplify は静的ウェブコンテンツ（例: HTML、CSS、JavaScript、イメージなどのファイル）を公開・配信するのに最適なサービスです。
エンドユーザーは AWS Amplify コンソールによって公開されているパブリックウェブサイトの URL を使用してサイトにアクセスします。
サイトを使用できるようにするため、ウェブサーバーを実行したり他のサービスを使用したりする必要はありません。

---

### ① リージョンの選択

AWS マネジメントコンソールで使用するリージョンを選択し、切り替えます。

### ② AWS CLI の確認

AWS CLI を使用するため、開発環境にインストールされていることを確認します。
インストールしていない場合は、ダウンロード・インストールします。

### ③ CodeCommit リポジトリの作成

ソースコードを管理するには、AWS CodeCommit を使用します。そのため Git リポジトリを作成します。

- AWS マネジメントコンソールで「AWS CodeCommit」コンソールを開きます。

### ④ リポジトリの設定

1. [リポジトリを作成] を選択します。
2. [リポジトリ名] には `wildrydes-site` と入力します。
3. [作成] を選択します。

### ⑤ IAM ユーザーのセットアップ

リポジトリが作成されたら、IAM コンソールで Git の認証情報を使用し IAM ユーザーをセットアップします。

1. IAM ページでユーザーを新規作成します。
2. 既存ポリシー（`AWSCodeCommitFullAccess`）をアタッチします。
3. マネジメントコンソールへのログインも許可するため、任意のパスワードを設定します。

### ⑥ Git のインストール

開発環境に Git がインストールされていない場合は、以下からインストールします。

- <https://git-scm.com/install/>

### ⑦ Git 認証情報の作成

IAM ユーザー用の Git 認証情報を作成します。

1. 作成済の IAM ユーザーを選択します。
2. 「セキュリティ認証情報」タブの「AWS CodeCommit の HTTPS Git 認証情報」を作成します。
3. 認証情報ファイルをダウンロードします。

### ⑧ AWS CLI の設定

AWS CLI がインストールされたターミナルウィンドウ（コマンドプロンプト）で以下を実行します。

```bash
aws configure
```

- 作成済の IAM ユーザーの AWS アクセスキー ID とシークレットアクセスキーを入力します。
  （CodeCommit の認証情報ではありません）
- デフォルトのリージョン名には、CodeCommit リポジトリを作成するために最初に選択したリージョンを入力します。
- デフォルトの出力形式は空白のままにして、Enter キーを押します。

### ⑨ Git 認証情報ヘルパーの設定

以下のコマンドを実行します。

```bash
git config --global credential.helper "!aws codecommit credential-helper $@"
git config --global credential.UseHttpPath true
```

> **※** これにより `.gitconfig` ファイルが生成され、次の情報が書き込まれます。
> Git が AWS CodeCommit にアクセスする際、ユーザー名・パスワードの代わりに IAM 認証情報が自動的に使われます。

### ⑩ 設定の検証

以下のコマンドを実行して、設定値がユーザープロファイルの `.gitconfig` ファイルに書き込まれていることを検証します。
（デフォルトでは `%HOME%\.gitconfig` または `drive:\Users\UserName\.gitconfig`）

```bash
git config --global --edit
```

成功した場合は、以下のような値が表示されます。
終了するには `:q` と入力して Enter を押すか、開いたエディタを閉じます。

```ini
[credential]
    helper = !aws codecommit credential-helper $@
    UseHttpPath = true
```

### ⑪ CodeCommit リポジトリの確認

1. 作成した IAM ユーザーで AWS コンソールへログインします。
2. 使用リージョンに切り替えます。
3. AWS CodeCommit ページの `wildrydes-site` リポジトリを選択します。

### ⑫ クローン URL のコピー

[URL のクローン] ドロップダウンから [HTTPS のクローン] を選択し、HTTPS URL をコピーします。

### ⑬ リポジトリのクローン

ターミナルウィンドウから `git clone` コマンドを実行し、リポジトリの HTTPS URL を貼り付けます。

```bash
git clone https://git-codecommit.us-east-1.amazonaws.com/v1/repos/wildrydes-site
```

> **※** `warning: You appear to have cloned an empty repository.`（空のリポジトリを複製した）という警告が表示されますが、想定通りです。

> **※** 認証情報が要求された場合は、作成済の IAM ユーザーの Git 認証情報
> （ダウンロード済の `ユーザー名_codecommit_credentials.csv` ファイルの情報）を入力します。

### ⑭ Git リポジトリの設定

ローカルでクローンを作成したら、ハンズオン用に事前に用意された S3 バケットからウェブサイトのコンテンツをコピーし、リポジトリにコンテンツを追加する必要があります。

そのため、作成済 IAM ユーザーが S3 へアクセスできる必要があるため、`AmazonS3FullAccess` ポリシーを追加設定します。

### ⑮ S3 からコンテンツをコピー

以下のコマンドを実行します。

```bash
cd wildrydes-site
aws s3 cp s3://ttt-wildrydes/wildrydes-site ./ --recursive
```

### ⑯ Git へのプッシュ

Git ファイルを追加、コミット、プッシュします。

```bash
git add .
git commit -m "new files"
git push
```

> **※** `git commit` で `Author identity unknown` エラーが出る場合は、メッセージに従い以下を実行します。
>
> ```bash
> git config --global user.email "your-email@example.com"
> git config --global user.name "Your Name"
> ```

> **※** `git push` でエラーが出る場合は、`git remote -v` コマンドで正しい URL かを確認します。
> 異なる場合は以下のコマンドで設定し直して再実行してください。
>
> ```bash
> git remote set-url origin https://git-codecommit.xxxxx.amazonaws.com/v1/repos/wildrydes-site
> ```

### ⑰ AWS Amplify でデプロイ

AWS Amplify コンソールを使用して、先ほど Git にコミットしたウェブサイトをデプロイします。
Amplify コンソールが静的ウェブアプリケーションコードを保存するための場所のセットアップ作業を実施し、アプリケーションのライフサイクルを簡略化します。

- AWS Amplify コンソールを起動し、[新しいアプリを作成] を選択します。

### ⑱ ソースプロバイダの選択

[AWS CodeCommit] を選択し、[次へ] をクリックします。

### ⑲ リポジトリの選択

[リポジトリの選択] ドロップダウンから [wildrydes-site] を選択します。

### ⑳ ブランチの選択

[ブランチ] ドロップダウンで [master] を選択し、[次へ] を選択します。

### ㉑ ビルド設定

[ビルド設定] ページでは、すべてデフォルトのままにして、[次へ] を選択し、作成します。

### ㉒ デプロイの待機

Amplify コンソールで必要なリソースが作成され、コードをデプロイするため、このプロセスには数分かかります。

### ㉓ デプロイの確認

「デプロイ済」になったことを確認し、指定された URL をクリックしサイトを起動します。

### ㉔ 変更の検出テスト

AWS Amplify コンソールは接続されたリポジトリで変更が検出されると、アプリを再ビルドし、再デプロイします。
このプロセスを確かめるためにメインページに変更を加えてみます。

- ローカルマシンで `wildrydes-site` フォルダに移動し、任意のテキストエディタで `index.html` ファイルを開きます。

### ㉕ タイトルの変更

タイトル行を次のテキストで変更します。

```html
<title>Wild Rydes - Rydes of the Future!</title>
```

### ㉖ ファイルの保存

ファイルを保存します。

### ㉗ 変更のプッシュ

ターミナルウィンドウで、変更を追加してコミットし、変更を Git リポジトリに再度プッシュします。

```bash
git add .
git commit -m "update title"
git push
```

Amplify コンソールはリポジトリへの変更を検出するとすぐにサイトのビルドを開始します。
「デプロイ済」から「デプロイ」にステータスが変更されています。

### ㉘ 再デプロイの確認

Amplify が再デプロイを完了したら、Wild Rydes サイトを再度開いて、タブのタイトルが変更されていることを確認します。
