# コマンドメモ

## AWS プロファイル追加

```bash
aws configure --profile <プロファイル名>
# 設定確認
aws sts get-caller-identity --profile <プロファイル名>
```

SSO の場合

```bash
aws configure sso --profile <プロファイル名>
```

## AWS プロファイル削除（手動）

AWS CLI にはプロファイルを丸ごと削除するコマンドがないため、設定ファイルを直接編集する。

```bash
# credentials から該当プロファイルのセクションを削除
vim ~/.aws/credentials

# config から該当プロファイルのセクションを削除
vim ~/.aws/config
```

---


### タスク5：CDK プロジェクト作成 & デプロイ

```bash
mkdir serverless-cdk
cd serverless-cdk
npx cdk init app --language typescript --generate-only
npm install
```

```bash
# 初回のみ：ブートストラップ
npx cdk bootstrap --profile serverless

# デプロイ
make deploy
```


### StepFunction 動作確認

```bash
aws events put-events --profile serverless --entries '[{"Source":"app.order","DetailType":"OrderCreated","Detail":"{\"orderId\":\"ORDER-001\",\"userId\":\"USER-01\",\"amount\":5000}"}]'
```
